.env where you can put the OperAI key

htmlTemplates : for the UI of the HTML page 

Run the command pip install -r requirements.txt to install the necessary Python packages.

after this RUN : streamlit run app.py --server.address 192.168.100.218 --server.port 8503


